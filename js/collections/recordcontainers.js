// Generated by CoffeeScript 1.6.3
define(['jquery', 'underscore', 'backbone', 'models/recordcontainer', 'collections/records', 'common', 'moment', 'backbone.indexedDB'], function($, _, Backbone, RecordContainer, Records, Common, moment) {
  'use strict';
  return Backbone.Collection.extend({
    model: RecordContainer,
    store: new Backbone.IndexedDB.Store('recordContainers'),
    initialize: function() {
      var _this = this;
      this.records = new Records;
      this.listenTo(this.records, 'write', this.recordsUpdate);
      this.listenTo(Common.targetDate, 'change', function() {
        return _this.recordsFetch();
      });
      return this;
    },
    getContainer: function() {
      var container;
      container = this.find(function(container) {
        return moment(container.getTime('contentTime')).isSame(Common.targetDate.date);
      });
      if (container) {
        return container;
      }
      container = new RecordContainer;
      container.set('contentTime', Common.targetDate.date.toString());
      this.add(container);
      container.save();
      return container;
    },
    recordsUpdate: function() {
      var container;
      container = this.getContainer();
      container.set('content', JSON.stringify(this.records.toJSON()));
      container.set('lastModifyTime', (new Date).toString());
      return container.save();
    },
    recordsFetch: function() {
      return this.records.reset(JSON.parse(this.getContainer().get('content')));
    }
  });
});
